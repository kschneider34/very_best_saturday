<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Favorites

    <a data-toggle="collapse" href="#dish_favorites_new_form">
      Add bookmark
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @dish.favorites.each do |bookmark| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <a href="/bookmarks/<%= bookmark.id %>">
          <%= bookmark.dish_id %>
        </a>

        <%if current_user == bookmark.user %>
          <a href="/delete_bookmark_from_dish/<%= bookmark.id %>">
            Delete
          </a>
        <% end %>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="dish_favorites_new_form">
        <%= render partial: "dish_templates/dish_favorites_new_form" %>
      </div>
    </div>

  </ul>
</div>
